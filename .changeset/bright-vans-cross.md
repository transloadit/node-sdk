---
'@transloadit/notify-url-proxy': minor
---

Add a new `@transloadit/notify-url-proxy` package for running a local Transloadit
`notify_url` proxy with fetch-based forwarding, assembly polling, retry logic, and a CLI/TUI.
